TO DO

Make different firewall rules for each linux system(web, ssh, whatever)
install fail2ban on each
check kernel version and install latest
script that checks all setuid bit binries
script that checks /etc/ n /tmp directories for new files etc
